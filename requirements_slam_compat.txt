# Additional packages needed in the `slam` conda env to run SAEMark evaluation.
# Assumes the slam env already has: torch, transformers, huggingface_hub,
#   scikit-learn, scipy, statsmodels, nltk
#
# Install with:
#   conda activate slam
#   pip install -r requirements_slam_compat.txt

nltk>=3.9
